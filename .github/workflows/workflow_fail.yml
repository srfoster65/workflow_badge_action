name: workflow fail

on:
  workflow_call:
  
jobs:
  workflow_fail:
    name: create badge
    runs-on: ubuntu-latest
    concurrency:
      group: badge
    
    steps:
    - name: create badge
      uses: srfoster65/workflow_badge_action@main
      with:
        label: ${{ github.job}}
        status: failure
